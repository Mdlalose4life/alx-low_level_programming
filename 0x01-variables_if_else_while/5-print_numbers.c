#include <stdio.h>
/**
 * main - Printing numbers from 0 to 9.
 *
 * Return: Always 0 (succss)
 */
int main(void)
{
int numb = 0;
	for ( ; numb < 10; numb++)
	{
	printf("%d", numb);
	}
printf("\n");
return (0);
}
