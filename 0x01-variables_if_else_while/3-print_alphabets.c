#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
main(void)
{
	int ch;

	for (ch = 'A' ; ch <= 'Z' ; Ch++)
	{
		putchar(ch);
	}
		putch('\n');
	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	return (0);
}
