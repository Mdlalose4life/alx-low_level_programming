#include <stdio.h>
/**
 * main - prints digits from 0 to 10 followed by a new line.
 * using a putchar.
 * Return: Always 0 (success)
 */
int main(void)
{
int numb = o;
	for (; numb < 10 ; numb++)
	{
		putchar((numb % 10) + '0');
	}
	putchar('\n');
	return (0);
}
