#include "main.h"

/**
 * _abs - returns absolute value of a number
 * @n: parameter to be computed
 * return: absolute value of a number
 */
int _abs(int n)
{
if (n >= 0)
	return (n);
else
	return (-n);
}
