#include "main.h"
#include <stdio.h>

/**
 *
 */
char *_strstr(char *haystack, char *needle)
{
	int i;
	
	if (*needle == 0)
		return 0;

	for(; *haystack; heystack++)
	{
		i = 0;
		
		if (haystack[i] == needle[i])
			do{
				if (needle[i + 1] == '\0')
					return (
			}
	}
}
